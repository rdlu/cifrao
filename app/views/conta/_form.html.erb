<%= form_for(@contum) do |f| %>
  <% if @contum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contum.errors.count, "error") %> prohibited this contum from being saved:</h2>

      <ul>
      <% @contum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :agencia %><br />
    <%= f.text_field :agencia %>
  </div>
  <div class="field">
    <%= f.label :nro_conta %><br />
    <%= f.text_field :nro_conta %>
  </div>
  <div class="field">
    <%= f.label :saldo %><br />
    <%= f.text_field :saldo %>
  </div>
  <div class="field">
    <%= f.label :entidades_id %><br />
    <%= f.text_field :entidades_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
